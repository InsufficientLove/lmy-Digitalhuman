# 域名（必须解析到本服务器IP）
DOMAIN=your.domain.com

# 用于Let's Encrypt的邮箱
ACME_EMAIL=your-email@example.com

# 指定可见GPU（示例：使用两张4090D）
CUDA_VISIBLE_DEVICES=0,1

# 镜像仓库（默认 GHCR）
REGISTRY=ghcr.io
NAMESPACE=YourGitHubUserOrOrg
PY_IMAGE=musetalk-python
WEB_IMAGE=lmy-digitalhuman
TAG=latest