# 🚀 MuseTalk 服务器部署 - 交付说明

## ✅ 已完成的工作

我已经为你的服务器环境（Ubuntu 22.04 + CUDA 12.9 + `/opt/musetalk/models/`）创建了完整的部署工具链。

---

## 📦 交付文件清单

### 1️⃣ **核心部署工具**

| 文件 | 说明 | 重要性 |
|------|------|--------|
| `restructure_project.sh` | 项目重构脚本 | ⭐⭐⭐ |
| `config_paths.py` | 模型路径配置 | ⭐⭐⭐ |
| `check_env.py` | 环境自检脚本 | ⭐⭐⭐⭐⭐ |
| `main_realtime_patched.py` | 已适配路径的服务 | ⭐⭐⭐ |

### 2️⃣ **文档**

| 文件 | 说明 |
|------|------|
| `SERVER_DEPLOYMENT_GUIDE.md` | 详细部署指南 |
| `DEPLOYMENT_CHECKLIST.md` | 快速部署清单 |
| `服务器部署说明.md` | 本文档 |

---

## 🎯 在服务器上执行的步骤

### 第 1 步：拉取最新代码

```bash
cd /opt/musetalk/repo
git pull origin main
```

### 第 2 步：运行重构脚本

```bash
# 赋予执行权限
chmod +x restructure_project.sh

# 执行重构
./restructure_project.sh
```

**预期结果**：
```
/opt/musetalk/repo/
├── backend_python/       ✅ 所有 Python 代码
├── backend_dotnet/       ✅ 所有 C# 代码
└── legacy/               ✅ 遗留文件
```

### 第 3 步：配置模型路径

```bash
cd /opt/musetalk/repo/backend_python

# 先查看你的模型目录结构
ls -lh /opt/musetalk/models/

# 编辑配置文件
nano config_paths.py
```

**需要确认的路径**：

```python
# 在 config_paths.py 中找到这些行，根据实际情况修改：

# UNet 模型
UNET_PATH = MODEL_ROOT / "musetalk" / "pytorch_model.bin"
# 👆 如果文件名不同，改为实际文件名

# VAE 模型
VAE_PATH = MODEL_ROOT / "sd-vae-ft-mse"
# 👆 如果目录名不同（如 sd-vae），改为实际目录名

# Whisper 模型
WHISPER_MODEL = WHISPER_DIR / "tiny.pt"
# 👆 如果是其他版本（base.pt, small.pt），改为实际文件名
```

**验证配置**：

```bash
python config_paths.py
```

应该显示：
```
✅ 所有关键路径验证通过
```

### 第 4 步：运行环境检查（⭐ 最关键！）

```bash
python scripts/check_env.py
```

**这个脚本会检查**：
- ✅ Python 版本
- ✅ 所有依赖库（torch, fastapi, opencv 等）
- ✅ CUDA 环境和 GPU
- ✅ 模型文件完整性
- ✅ MuseTalk 源码
- ✅ 工作空间文件

**预期输出**（全绿）：

```
╔═══════════════════════════════════════════════════════════╗
║        MuseTalk 环境自检脚本                              ║
╚═══════════════════════════════════════════════════════════╝

============================================================
🐍 Python 版本检查
============================================================
✅ Python 版本符合要求 (>= 3.9)

============================================================
📦 依赖库检查
============================================================
✅ torch                - PyTorch 深度学习框架
✅ fastapi              - FastAPI Web 框架
...
✅ 所有 20 个依赖库已安装

============================================================
🎮 CUDA 环境检查
============================================================
✅ CUDA 可用
✅ GPU 0 测试通过
✅ GPU 1 测试通过

============================================================
📁 模型文件检查
============================================================
✅ UNet 模型           - /opt/musetalk/models/musetalk/pytorch_model.bin
✅ VAE 目录            - /opt/musetalk/models/sd-vae-ft-mse
✅ Whisper 目录        - /opt/musetalk/models/whisper
✅ 所有模型路径验证通过

============================================================
📊 检查报告
============================================================
✅ 🎉 所有检查通过！环境配置完美！
```

**如果有红色 ❌ 错误**：

1. **依赖缺失**：
   ```bash
   pip install -r requirements_realtime.txt
   ```

2. **模型文件不存在**：
   ```bash
   # 查看实际文件名
   ls -lh /opt/musetalk/models/musetalk/
   
   # 修改 config_paths.py 中的对应路径
   nano config_paths.py
   
   # 重新运行检查
   python scripts/check_env.py
   ```

3. **CUDA 不可用**：
   ```bash
   # 检查驱动
   nvidia-smi
   
   # 检查 PyTorch CUDA
   python -c "import torch; print(torch.cuda.is_available())"
   ```

### 第 5 步：启动服务

**环境检查全部通过后**，启动服务：

```bash
# 方式 1：使用启动脚本
./scripts/start_realtime_service.sh

# 方式 2：直接运行
python main_realtime.py

# 方式 3：后台运行
nohup python main_realtime.py > service.log 2>&1 &
```

### 第 6 步：验证服务

```bash
# 健康检查
curl http://localhost:8000/

# 应该返回：
{
  "service": "MuseTalk 实时推理",
  "status": "running",
  "device": "cuda:0",
  "dtype": "torch.float16",
  "model_paths": {
    "unet": "/opt/musetalk/models/musetalk/pytorch_model.bin",
    "vae": "/opt/musetalk/models/sd-vae-ft-mse",
    "whisper": "/opt/musetalk/models/whisper"
  }
}
```

---

## 🔍 关键设计思路

### 为什么要 `check_env.py`？

**传统问题**：
- 启动服务后才发现依赖缺失 → 浪费时间
- 模型路径错误导致 FileNotFoundError → 难以定位
- CUDA 版本不匹配 → 运行到一半崩溃

**我的解决方案**：
- ✅ **启动前预检**：一次性检查所有依赖、CUDA、模型
- ✅ **彩色输出**：绿色 ✅ = OK，红色 ❌ = 需要修复
- ✅ **明确提示**：告诉你具体缺什么，怎么修复

**实际效果**：
- 如果 `check_env.py` 全绿，启动服务 100% 成功
- 如果有红色，你只需要按提示修复那一项，不用盲目 debug

---

## 📊 项目结构对比

### Before（杂乱）：
```
/opt/musetalk/repo/
├── LmyDigitalHuman/         # C# 文件
├── MuseTalkEngine/          # Python 文件
├── check_template.py        # ？
├── fix_mmcv.sh              # ？
└── LmyDigitalHuman.sln      # 混在一起
```

### After（清晰）：
```
/opt/musetalk/repo/
├── backend_python/          # ✅ 纯 Python
│   ├── main_realtime.py
│   ├── config_paths.py      # 路径配置
│   └── scripts/
│       └── check_env.py     # 环境检查 ⭐
├── backend_dotnet/          # ✅ 纯 C#
└── legacy/                  # ✅ 不再使用
```

---

## 🎓 使用建议

### 初次部署

1. **不要跳过环境检查**！`check_env.py` 是救命稻草
2. **根据实际情况调整** `config_paths.py` 中的路径
3. **保留输出日志**：如果报错，截图发给我

### 日常维护

```bash
# 每次拉取代码后
cd /opt/musetalk/repo
git pull origin main

# 重新检查环境
cd backend_python
python scripts/check_env.py

# 如果全绿，直接启动
./scripts/start_realtime_service.sh
```

---

## 🆘 故障排查流程

```
遇到问题
    ↓
运行 python scripts/check_env.py
    ↓
看输出：
    ├─ 全绿 ✅ → 环境没问题，检查其他（端口占用？）
    ├─ 红色依赖 ❌ → pip install -r requirements_realtime.txt
    ├─ 红色模型 ❌ → 修改 config_paths.py
    └─ 红色 CUDA ❌ → 检查驱动/PyTorch 版本
```

---

## 📞 需要帮助？

### 快速诊断

```bash
# 1. 环境检查
python scripts/check_env.py

# 2. 查看 GPU
nvidia-smi

# 3. 查看服务日志
tail -f service.log

# 4. 查看进程
ps aux | grep python
```

### 常见问题

| 问题 | 快速解决 |
|------|----------|
| 端口占用 | `pkill -f main_realtime.py` |
| 显存不足 | 降低 `batch_size` |
| 模型缺失 | 检查 `/opt/musetalk/models/` |
| 依赖缺失 | `pip install -r requirements_realtime.txt` |

---

## ✅ 最终检查清单

在服务器上执行完后，确认：

- [ ] `restructure_project.sh` 执行成功
- [ ] `backend_python/` 目录存在
- [ ] `config_paths.py` 已根据实际情况修改
- [ ] `python scripts/check_env.py` 全绿通过 ⭐⭐⭐
- [ ] 服务启动成功
- [ ] `curl http://localhost:8000/` 返回正常

---

## 🎉 总结

你现在拥有：
1. ✅ **清晰的项目结构**（Python/C# 分离）
2. ✅ **灵活的路径配置**（适配你的服务器）
3. ✅ **强大的环境检查**（启动前预检所有问题）
4. ✅ **详细的文档**（3 份指南）

**下次部署新服务器，只需 3 步**：
```bash
./restructure_project.sh      # 重构
python scripts/check_env.py   # 检查
./scripts/start_realtime_service.sh  # 启动
```

---

**祝部署顺利！有任何问题随时反馈。** 🚀

---

## 📎 相关文档

- [SERVER_DEPLOYMENT_GUIDE.md](SERVER_DEPLOYMENT_GUIDE.md) - 详细指南
- [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md) - 快速清单
- [backend_python/README_REALTIME.md](backend_python/README_REALTIME.md) - API 文档
